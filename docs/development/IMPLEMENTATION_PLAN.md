# cldev (Claude Dev CLI) 実装計画書

**バージョン**: 2.0.0
**作成日**: 2025-11-07
**最終更新**: 2025-11-07
**ステータス**: 承認待ち

**重要変更**:
- コマンド数: 41 → 29（29%削減）
- コマンド名: `claude` → `cldev`（Claude Code CLI本体との競合回避）
- 入力補完、i18n、初期設定機能を追加
- 設定ファイル編集機能を追加（config edit）
- セキュリティ設計強化（Phase 1-A必須）

---

## 📋 目次

1. [プロジェクト概要](#プロジェクト概要)
2. [最適化されたコマンド構成](#最適化されたコマンド構成)
3. [新機能要件](#新機能要件)
4. [アーキテクチャ設計](#アーキテクチャ設計)
5. [ディレクトリ構造](#ディレクトリ構造)
6. [段階的実装計画](#段階的実装計画)
7. [技術スタック](#技術スタック)
8. [コマンド仕様](#コマンド仕様)
9. [CI/CD設計](#cicd設計)
10. [国際化対応](#国際化対応)
11. [配布戦略](#配布戦略)
12. [テスト戦略](#テスト戦略)
13. [マイグレーション計画](#マイグレーション計画)
14. [リスク評価](#リスク評価)
15. [成功基準](#成功基準)

---

## プロジェクト概要

### 🎯 目的

Claude Code開発環境の**3つの独立したシェルスクリプト群**を、**1つの統合Rust CLIツール `cldev`** に集約し、保守性・配布性・ユーザー体験を向上させる。

### 📊 現状の課題と解決策

**課題**:
- 分散したコマンド体系（41コマンド、3エントリポイント）
- bash 5.x依存（macOS標準bash 3.xで動作不可）
- 配布の複雑性（手動インストール、環境変数設定）
- 保守性（型安全性なし、エラーハンドリング不十分）

**解決策**:
```bash
# 単一エントリポイント（最適化版：29コマンド）
cldev config check         # 元: ~/.claude/scripts/claude validate
cldev dev feature          # 元: uc feature
cldev lr find "暗号化"     # 元: ~/.claude/learning-analytics/context-search.sh
cldev git merge-request    # 元: uc pr / uc mr（自動検出統合）
cldev tech start           # 元: uc web:dev / api:dev等（自動検出統合）
```

### 💡 期待効果

| 項目 | 現状 | 統合後 | 改善率 |
|------|------|--------|--------|
| **コマンド数** | 41コマンド | 29コマンド | **29%削減** |
| **コマンド長** | `~/.claude/scripts/claude validate` (35文字) | `cldev cv` (8文字) | **77%削減** |
| **インストール** | 手動設定（5ステップ） | `cargo install cldev` or `brew install cldev` | **80%削減** |
| **起動速度** | bash起動 (50-100ms) | Rustバイナリ (5-10ms) | **90%高速化** |
| **保守性** | bash型なし | Rust型安全・詳細エラー | **品質向上** |
| **多言語対応** | なし | 英語・日本語完全対応 | **UX向上** |
| **入力補完** | なし | Zsh/Bash/Fish対応 | **UX向上** |

---

## 最適化されたコマンド構成

### 📊 全体構成（29コマンド、9カテゴリ）

```yaml
config (6コマンド):
  - init          # 🆕 初期設定（対話的セットアップ）
  - check         # validate + check統合（--validate, --health）
  - edit          # 🆕 設定ファイルをエディタで開く
  - maintain      # 月次メンテナンス
  - list          # 全コマンド一覧
  - update-docs   # ドキュメント管理（gitから移動）

dev (7コマンド):
  - feature       # 新機能実装フロー
  - debug         # 体系的デバッグ
  - fix           # 重要バグ修正
  - urgent        # 本番障害対応
  - optimize      # パフォーマンス最適化
  - refactor      # リファクタリング
  - research      # 技術調査・学習記録

git (4コマンド):
  - commit        # 規約準拠コミット
  - branch        # ブランチ作成
  - merge-request # PR/MR作成（自動検出: GitHub/GitLab）
  - status        # Git状態確認 + 推奨アクション

quality (3コマンド):
  - lint          # コード品質チェック（プロジェクト自動検出）
  - format        # コード整形（プロジェクト自動検出）
  - test          # テスト実行（プロジェクト自動検出）

tech (1コマンド):
  - start [type]  # 開発環境起動（web/api/mobile/ds、自動検出）

ops (2コマンド):
  - build         # ビルド実行（プロジェクト自動検出）
  - deploy        # デプロイ実行

analysis (4コマンド):
  - analyze       # コード分析（analyze-code → 短縮）
  - explain       # 技術説明
  - review-mr     # MR/PRレビュー
  - serena        # セマンティック解析（MCP統合、opsから移動）

lr (4コマンド):
  - find          # 検索（search, recent, recommend統合）
  - stats         # 統計（stats, tags, patterns, measure統合）
  - problems      # 問題検出
  - new           # 新規学習記録作成

todo (1コマンド):
  - manage        # todo管理（add/list/complete、gitから独立）

---
合計: 29コマンド（41 → 29、29%削減）
```

### 🔍 最適化の詳細

**削減されたコマンド（13コマンド）**:
1. `config validate` → `config check --validate`
2. `config smart` → Phase 1から除外（将来拡張）
3. `git pr` → `git merge-request`（自動検出）
4. `git mr` → `git merge-request`（自動検出）
5. `tech web-dev` → `tech start web`
6. `tech web-build` → `ops build`
7. `tech api-dev` → `tech start api`
8. `tech mobile-dev` → `tech start mobile`
9. `tech ds-notebook` → `tech start ds`
10. `lr search` → `lr find`
11. `lr recent` → `lr find --recent`
12. `lr recommend` → `lr find --recommend`
13. `lr tags` → `lr stats --tags`

**追加されたコマンド（1コマンド）**:
1. `config init` - 初期設定（🆕）

---

## 新機能要件

### 1. 🆕 初期設定コマンド（`cldev init`）

```bash
cldev init

# 対話的セットアップフロー（シンプル版）
cldev - Initial Setup
━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Language / 言語
   > English / 日本語
   [日本語]

2. Claude Code directory
   ~/.claude/ directory
   ✓ Detected: /Users/sanae/.claude

3. Project root
   Development projects directory
   [~/projects]

4. Git CLI
   ✓ GitHub CLI (gh): detected
   - GitLab CLI (glab): not found

5. Shell completion
   Shell: zsh
   Add to: ~/.zshrc

6. Aliases
   Add shortcut 'c=cldev'
   [yes]

━━━━━━━━━━━━━━━━━━━━━━━━━━

✓ Configuration saved: ~/.config/cldev/config.toml
✓ Shell completion added: ~/.zshrc
✓ Aliases added: ~/.zshrc

Next: source ~/.zshrc
```

**機能**:
- 言語選択（英語/日本語）
- Claude Code環境検出
- Git CLI確認（gh/glab）
- シェル補完設定
- エイリアス設定
- 設定ファイル生成（TOML）

### 2. 📝 設定ファイル（TOML形式 + バージョニング）

**場所**: `~/.config/cldev/config.toml`

**設定バージョニング戦略**:
- セマンティックバージョニング（major.minor.patch）
- メジャーバージョン変更: 互換性なし（マイグレーション必須）
- マイナーバージョン変更: 後方互換性あり（新フィールド追加）
- パッチバージョン変更: 完全互換性（バグ修正のみ）

```toml
# cldev 設定ファイル
version = "1.0.0"  # 設定ファイルバージョン（必須）

[general]
language = "ja"  # en or ja
claude_dir = "/Users/sanae.abe/.claude"
projects_dir = "/Users/sanae.abe/projects"

[git]
github_cli = true
gitlab_cli = false
default_base_branch = "main"
auto_push = true

[quality]
auto_fix = false
run_tests_before_commit = true

[dev]
auto_create_branch = true
branch_prefix = "feature"
session_recording = true

[lr]
sessions_dir = "/Users/sanae.abe/.claude/learnings"
auto_save = true
default_tags = ["development", "claude-code"]

[ui]
color = true
emoji = true
progress_bar = true

[performance]
parallel_tasks = 4
timeout_seconds = 300
```

### 3. 🌐 国際化対応（i18n）

**対応言語**: 英語（en）、日本語（ja）

**実装方法**:
- Phase 1-B: JSON-based i18n（シンプル、学習曲線緩やか）
- Phase 4以降: fluent-rs検討（高度な多言語対応）

**使用例**:
```bash
# 英語
$ cldev config check
✅ Configuration file is valid
💡 Next step: cldev dev feature

# 日本語
$ cldev config check
✅ 設定ファイルは正常です
💡 次のステップ: cldev dev feature
```

### 4. 🔒 セキュリティ設計（Phase 1-A必須）

**セキュリティ原則**:
- 多層防御（Defense in Depth）
- 最小権限の原則
- 入力検証の徹底
- セキュアデフォルト

**実装内容**:

#### 1. パストラバーサル防止（SecurePath）
- パス正規化（canonicalize）
- ベースディレクトリ外アクセス拒否
- 設定ファイルパーミッション検証（600）

#### 2. コマンドインジェクション防止（safe_command）
- std::process::Command使用
- シェル展開無効化
- 許可されたコマンドのみ実行

### 5. 🔧 入力補完（Shell Completion）

**対応シェル**: Bash, Zsh, Fish, PowerShell

**生成方法**:
```bash
# Zsh
cldev completions zsh > ~/.zsh/completions/_cldev

# Bash
cldev completions bash > /usr/local/etc/bash_completion.d/cldev

# Fish
cldev completions fish > ~/.config/fish/completions/cldev.fish
```

**補完内容**:
- サブコマンド補完
- オプション補完
- ファイルパス補完
- Git ブランチ補完
- 学習記録タグ補完

**実装**: `clap_complete` クレート使用

---

## アーキテクチャ設計

### 🏗️ システム構成図

```
┌─────────────────────────────────────────────────────┐
│                   cldev CLI                         │
│              (Rust Binary + i18n)                   │
├─────────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐         │
│  │ config   │  │   dev    │  │   git    │         │
│  │ (6 cmds) │  │ (7 cmds) │  │ (4 cmds) │         │
│  └──────────┘  └──────────┘  └──────────┘         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐         │
│  │ quality  │  │   tech   │  │   ops    │         │
│  │ (3 cmds) │  │ (1 cmd)  │  │ (2 cmds) │         │
│  └──────────┘  └──────────┘  └──────────┘         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐         │
│  │ analysis │  │    lr    │  │   todo   │         │
│  │ (4 cmds) │  │ (4 cmds) │  │ (1 cmd)  │         │
│  └──────────┘  └──────────┘  └──────────┘         │
├─────────────────────────────────────────────────────┤
│              Core Libraries                         │
│  ┌─────────────────────────────────────────────┐   │
│  │ Config: TOML読み込み・設定管理               │   │
│  │ I18n: 多言語対応（JSON-based → fluent-rs）  │   │
│  │ Completion: シェル補完生成                  │   │
│  │ ProjectDetector: 自動検出（Node/Rust/Go等） │   │
│  │ SessionRecorder: 学習記録自動保存           │   │
│  │ GitUtils: Git操作・リモート検出             │   │
│  │ Security: SecurePath/safe_command           │   │
│  └─────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
          ↓                    ↓                ↓
  ┌──────────────┐    ┌──────────────┐  ┌──────────┐
  │ ~/.config/   │    │ Git (gh/glab)│  │ External │
  │ cldev/       │    │              │  │ Commands │
  │ config.toml  │    │              │  │          │
  └──────────────┘    └──────────────┘  └──────────┘
```

### 🔧 モジュール設計

```rust
src/
├── main.rs                     // エントリポイント
├── cli/                        // CLI定義
│   ├── mod.rs
│   ├── args.rs                 // clap引数定義
│   ├── output.rs               // 出力フォーマット（多言語対応）
│   └── completions.rs          // 補完生成
├── commands/                   // 全コマンド（29種類）
│   ├── mod.rs
│   ├── config/                 // 6コマンド
│   │   ├── init.rs             // 🆕 初期設定ウィザード
│   │   ├── check.rs            // 設定検証
│   │   ├── edit.rs             // 🆕 設定ファイルエディタで開く
│   │   ├── maintain.rs
│   │   ├── list.rs
│   │   └── update_docs.rs
│   ├── dev/                    // 7コマンド
│   ├── git/                    // 4コマンド
│   │   ├── merge_request.rs    // PR/MR統合（自動検出）
│   │   └── ...
│   ├── quality/                // 3コマンド
│   ├── tech/                   // 1コマンド
│   │   └── start.rs            // 開発環境起動（自動検出）
│   ├── ops/                    // 2コマンド
│   ├── analysis/               // 4コマンド
│   ├── lr/                     // 4コマンド
│   │   ├── find.rs             // 検索統合
│   │   ├── stats.rs            // 統計統合
│   │   └── ...
│   └── todo/                   // 1コマンド
├── core/                       // コアライブラリ
│   ├── config.rs               // TOML設定管理
│   ├── i18n.rs                 // 国際化（JSON-based）
│   ├── security.rs             // セキュリティ（SecurePath/safe_command）
│   ├── project_detector.rs     // プロジェクト自動検出
│   ├── session_recorder.rs
│   ├── git_utils.rs
│   └── file_utils.rs
├── i18n/                       // 多言語リソース
│   ├── messages.json           // JSON-based（Phase 1-B）
│   └── (fluent files)          // fluent-rs（Phase 4以降検討）
└── error.rs                    // エラー定義
```

---

## ディレクトリ構造

### 📁 完全なプロジェクト構造

```
~/projects/cldev/
├── Cargo.toml
├── Cargo.lock
├── README.md
├── LICENSE
├── CHANGELOG.md
├── IMPLEMENTATION_PLAN.md   // 本ドキュメント
│
├── src/
│   ├── main.rs
│   ├── cli/
│   ├── commands/
│   ├── core/
│   ├── i18n/               // 🆕 多言語リソース
│   │   └── messages.json
│   └── error.rs
│
├── tests/
│   ├── integration/
│   ├── security/           // 🆕 セキュリティテスト
│   └── fixtures/
│
├── benches/
│   └── command_performance.rs
│
├── docs/                   // 🆕 ドキュメント集約
│   ├── README.md
│   ├── architecture/
│   ├── user-guide/
│   ├── development/
│   └── i18n/
│
├── completions/            // 🆕 シェル補完スクリプト
│   ├── cldev.bash
│   ├── cldev.zsh
│   ├── cldev.fish
│   └── _cldev.ps1
│
├── .github/
│   └── workflows/
│       ├── ci.yml
│       ├── release.yml
│       └── security.yml
│
└── scripts/
    ├── build.sh
    ├── test.sh
    └── release.sh
```

---

## 段階的実装計画

### 🎯 Phase 1-A: コア基盤（1週間）

**目標**: CLI基盤 + 設定管理 + セキュリティ + 基本コマンド

**実装内容**:

```yaml
1.1 プロジェクトセットアップ:
  - Cargo.toml最小構成（6クレートのみ）
    - clap, serde, toml, anyhow, thiserror, dirs
  - GitHub Actions CI設定（基本）

1.2 CLI基盤:
  - src/main.rs: エントリポイント
  - src/cli/args.rs: clap 4.x引数定義（29コマンド構造）
  - src/cli/output.rs: シンプル出力（カラー対応）
  - src/error.rs: エラー型階層（thiserror）

1.3 設定管理（バージョニング対応）:
  - src/core/config.rs: TOML読み込み・書き込み
  - ~/.config/cldev/config.toml構造定義
  - 設定バージョニング実装
    - CONFIG_VERSION: "1.0.0"
    - バージョン検証機能
    - マイグレーション基盤

1.4 セキュリティ基盤:
  - src/core/security.rs: セキュリティモジュール
    - SecurePath: パストラバーサル防止
    - safe_command(): コマンドインジェクション防止
    - 設定ファイルパーミッション検証（600）
  - セキュリティテスト実装

1.5 所有権戦略（Arc<Config>）:
  - Arc<T>ベースの設定共有
  - モジュール間での効率的な設定受け渡し
  - 並行実行時の安全性保証

1.6 基本コマンド実装（検証用）:
  - cldev config check
  - cldev config edit
  - cldev config list
  - cldev --version
  - cldev --help
```

**成果物**:
- 実行可能バイナリ `cldev`（最小構成）
- 設定ファイル基本機能
- セキュリティ基盤
- 単体テスト（カバレッジ80%以上）

**成功基準**:
- [ ] `cargo build --release` 成功
- [ ] `cldev config check` で設定検証（バージョニング含む）
- [ ] `cldev config edit` でエディタ起動
- [ ] SecurePath/safe_commandのセキュリティテスト通過
- [ ] CI/CD基本パイプライン動作

---

### 🚀 Phase 1-B: 高度機能（1週間）

**目標**: i18n + シェル補完 + 対話的UI

**実装内容**:

```yaml
1.1 シンプルi18n（JSON-based）:
  - src/core/i18n.rs: JSON-based多言語対応
  - src/i18n/messages.json:
    - 英語（en）
    - 日本語（ja）
  - 言語自動検出（環境変数LANG）

1.2 シェル補完:
  - src/cli/completions.rs: clap_complete統合
  - Zsh/Bash/Fish/PowerShell補完スクリプト生成

1.3 対話的UI:
  - dialoguerクレート追加
  - cldev config init: 対話的ウィザード

1.4 依存関係追加（Phase 1-B）:
  - clap_complete, dialoguer, colored, indicatif

1.5 統合テスト:
  - `cldev config init`フルフロー
  - 多言語出力確認
  - シェル補完動作確認
```

**成果物**:
- 多言語対応（英語・日本語）
- シェル補完動作（4シェル）
- 対話的初期設定ウィザード

**成功基準**:
- [ ] `cldev config init` で対話的設定完了
- [ ] `cldev --lang ja config check` で日本語出力
- [ ] `cldev completions zsh` で補完スクリプト生成
- [ ] Zsh/Bash/Fishでタブ補完動作確認

---

### 🚀 Phase 2: 高頻度コマンド（2週間）

**目標**: 日常的に使う頻度の高いコマンド実装（P0）

**実装内容**:

```yaml
2.1 Git操作コマンド（4コマンド）:
  - git commit: 規約準拠コミット
  - git branch: Conventional Branch作成
  - git merge-request: GitHub/GitLab自動検出
    - .git/config からリモートURL取得
    - github.com → gh pr create
    - gitlab.com → glab mr create
  - git status: Git状態確認 + 推奨アクション

2.2 品質管理コマンド（3コマンド）:
  - quality lint: プロジェクト検出 → ESLint/Clippy等
  - quality format: プロジェクト検出 → Prettier/rustfmt等
  - quality test: プロジェクト検出 → Vitest/Jest/cargo test等

2.3 緊急対応コマンド（3コマンド）:
  - dev urgent: 本番障害対応フロー
  - dev fix: 重要バグ修正フロー
  - dev debug: 体系的デバッグフロー

2.4 プロジェクト検出強化:
  - ProjectDetector trait実装
  - 各プロジェクトタイプの検出ロジック
```

**成果物**:
- 10コマンド実装完了
- プロジェクト自動検出機能
- Git リモート自動検出

**成功基準**:
- [ ] `cldev git commit` でConventional Commits形式コミット
- [ ] `cldev git merge-request` でGitHub/GitLab自動判定
- [ ] `cldev quality lint` でプロジェクトタイプ自動検出

---

### 🛠️ Phase 3: 開発フローコマンド（2週間）

**目標**: 開発ワークフロー支援コマンド実装

**実装内容**:

```yaml
3.1 開発コマンド（4コマンド）:
  - dev feature: 新機能実装フロー
  - dev refactor: リファクタリングフロー
  - dev optimize: パフォーマンス最適化フロー
  - dev research: 技術調査・学習記録

3.2 学習記録コマンド（4コマンド）:
  - lr find: 統合検索
  - lr stats: 統合統計
  - lr problems: 問題検出
  - lr new: 新規学習記録作成

3.3 セッション記録機能:
  - SessionRecorder実装
  - 学習記録自動保存
```

**成果物**:
- 8コマンド実装完了
- 学習記録システム統合

**成功基準**:
- [ ] `cldev dev feature user-auth` で5ステップ実行
- [ ] `cldev lr find "暗号化" --recent 5` で統合検索
- [ ] セッション記録が ~/.claude/learnings/ に保存

---

### 🌐 Phase 4: 技術スタック・分析コマンド（2週間）

**目標**: 技術スタック別環境起動・分析コマンド実装

**実装内容**:

```yaml
4.1 技術スタックコマンド（1コマンド）:
  - tech start [type]: 開発環境起動（自動検出）

4.2 運用コマンド（2コマンド）:
  - ops build: ビルド実行（プロジェクト自動検出）
  - ops deploy: デプロイ実行

4.3 分析コマンド（4コマンド）:
  - analysis analyze: コード分析
  - analysis explain: 技術説明
  - analysis review-mr: MR/PRレビュー
  - analysis serena: セマンティック解析（MCP統合）

4.4 todoコマンド（1コマンド）:
  - todo manage [add|list|complete]
```

**成果物**:
- 8コマンド実装完了
- 技術スタック別環境起動機能

**成功基準**:
- [ ] `cldev tech start` でプロジェクト自動検出・起動
- [ ] `cldev ops build` でプロジェクトタイプ別ビルド
- [ ] 全29コマンド動作確認

---

### 📦 Phase 5: 配布・最終調整（1週間）

**目標**: 配布準備とドキュメント整備

**実装内容**:

```yaml
5.1 配布パッケージ作成:
  - Homebrew Formula作成
  - cargo install対応
  - バイナリリリース（GitHub Releases）

5.2 ドキュメント整備:
  - README.md完全版（英語・日本語）
  - docs/user-guide/: 全コマンドリファレンス
  - docs/development/migration-guide.md

5.3 マイグレーション支援:
  - 既存エイリアス削除スクリプト
  - 新しいエイリアス設定スクリプト

5.4 最終調整:
  - パフォーマンス最適化
  - エラーメッセージ多言語化完全化
```

**成果物**:
- Homebrew Formula（`brew install cldev`）
- crates.io公開（`cargo install cldev`）
- GitHub Releases（バイナリ配布）
- 完全なドキュメント（英語・日本語）

**成功基準**:
- [ ] `brew install cldev` でインストール可能
- [ ] `cargo install cldev` でインストール可能
- [ ] 全29コマンド動作確認
- [ ] ドキュメント完全性100%（英語・日本語）
- [ ] 既存スクリプトからの移行成功

---

## 技術スタック

### 🦀 Rust依存関係

```toml
[package]
name = "cldev"
version = "1.0.0"
edition = "2021"
rust-version = "1.70"

[dependencies]
# CLI Framework
clap = { version = "4.5", features = ["derive", "cargo", "wrap_help", "string"] }
clap_complete = "4.5"

# Output & Formatting
colored = "2.1"
indicatif = "0.17"
comfy-table = "7.1"
dialoguer = "0.11"

# File Operations
walkdir = "2.5"
regex = "1.10"
glob = "0.3"

# Serialization（TOML対応）
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"

# Error Handling
anyhow = "1.0"
thiserror = "1.0"

# Date/Time
chrono = "0.4"

# System Info
home = "0.5"
which = "6.0"
dirs = "5.0"

[dev-dependencies]
assert_cmd = "2.0"
predicates = "3.1"
tempfile = "3.10"
pretty_assertions = "1.4"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
panic = "abort"
```

### 🛠️ 外部ツール依存

```yaml
必須:
  - git: バージョン管理

オプション:
  - gh: GitHub CLI
  - glab: GitLab CLI

プロジェクト検出用:
  - node/npm: Node.jsプロジェクト
  - cargo: Rustプロジェクト
  - go: Goプロジェクト
  - python: Pythonプロジェクト
```

---

## コマンド仕様

### 📋 コマンド構造

```bash
cldev <category> <command> [args] [options]

# 例
cldev config init
cldev config check --validate
cldev dev feature user-auth
cldev git commit "feat: add authentication"
cldev git merge-request  # 自動検出（GitHub/GitLab）
cldev quality lint --fix
cldev tech start         # 自動検出（web/api/mobile/ds）
cldev lr find "暗号化" --recent 5
```

### 🎨 出力フォーマット（多言語対応）

**UI設計原則**:
- ✅ シンプル・見やすさ優先
- ❌ 華美な装飾・明るすぎる色は禁止
- ✅ 落ち着いたカラーパレット
- ✅ 絵文字は最小限（ステータス表示のみ）

**出力例**:

```bash
# 英語（シンプル版）
$ cldev config check
✓ Configuration file is valid
  Checked: 10 settings
  Next: cldev dev feature

# 日本語（シンプル版）
$ cldev config check
✓ 設定ファイルは正常です
  確認済み: 10個の設定
  次: cldev dev feature
```

### 🚀 グローバルオプション

```bash
--verbose, -v        # 詳細出力
--quiet, -q          # 静かな出力（エラーのみ）
--no-color           # カラー出力無効
--help, -h           # ヘルプ表示
--version, -V        # バージョン表示
--dry-run            # ドライラン（実際には実行しない）
--json               # JSON形式出力
--lang <LANG>        # 言語指定（en/ja）
```

### 📝 重要コマンド詳細仕様

#### cldev config init（🆕）

```bash
cldev config init [--lang <LANG>] [--non-interactive]

機能: 初期設定ウィザード
  - 言語選択（英語/日本語）
  - Claude Code環境検出
  - Git CLI確認（gh/glab）
  - シェル補完設定
  - エイリアス設定
  - 設定ファイル生成（~/.config/cldev/config.toml）

オプション:
  --lang <LANG>: 言語指定（en/ja）
  --non-interactive: 対話なし（デフォルト値使用）
  --force: 既存設定を上書き
```

#### cldev config edit（🆕）

```bash
cldev config edit [--editor <EDITOR>]

機能: 設定ファイルをエディタで開く
  - ~/.config/cldev/config.toml を開く
  - $EDITOR環境変数のエディタを使用

オプション:
  --editor <EDITOR>: 使用するエディタを指定
  --create: 設定ファイルが存在しない場合は作成
```

#### cldev git merge-request（統合版）

```bash
cldev git merge-request [--title <TITLE>] [--body <BODY>]

機能: GitHub PR / GitLab MR作成（自動検出）
  - .git/config からリモートURL取得
  - github.com検出 → gh pr create
  - gitlab.com検出 → glab mr create

オプション:
  --title <TITLE>: PR/MRタイトル
  --body <BODY>: PR/MR本文
  --draft: ドラフトPR/MR作成

エイリアス: pr, mr
```

#### cldev tech start（統合版）

```bash
cldev tech start [TYPE] [--port <PORT>]

機能: 開発環境起動（プロジェクト自動検出）
  - TYPE省略: プロジェクト自動検出
  - TYPE指定: 明示的起動（web/api/mobile/ds）

オプション:
  --port <PORT>: ポート番号指定
  --detach: バックグラウンド起動
```

---

## CI/CD設計

### 🔄 GitHub Actions ワークフロー

```yaml
# .github/workflows/ci.yml
name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          components: rustfmt, clippy
      - name: Check formatting
        run: cargo fmt -- --check
      - name: Run clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        rust: [stable, beta]
    steps:
      - uses: actions/checkout@v4
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: ${{ matrix.rust }}
      - name: Run tests
        run: cargo test --all-features --verbose

  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions-rs/audit-check@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
```

---

## 国際化対応

### 🌐 言語リソース構造

#### JSON-based（Phase 1-B）

```json
{
  "en": {
    "command-success": "Command executed successfully",
    "config-check-success": "Configuration file is valid",
    "next-step": "💡 Next step: {command}"
  },
  "ja": {
    "command-success": "コマンド実行成功",
    "config-check-success": "設定ファイルは正常です",
    "next-step": "💡 次のステップ: {command}"
  }
}
```

---

## 配布戦略

### 📦 配布チャネル

#### 1. Homebrew（macOS/Linux）

```ruby
class Cldev < Formula
  desc "Unified CLI for Claude Code development environment"
  homepage "https://github.com/yourusername/cldev"
  url "https://github.com/yourusername/cldev/archive/v1.0.0.tar.gz"
  sha256 "..."
  license "MIT"

  depends_on "rust" => :build

  def install
    system "cargo", "install", *std_cargo_args
    bash_completion.install "completions/cldev.bash" => "cldev"
    zsh_completion.install "completions/cldev.zsh" => "_cldev"
    fish_completion.install "completions/cldev.fish"
  end
end
```

#### 2. crates.io

```bash
cargo publish
cargo install cldev
```

#### 3. GitHub Releases（バイナリ配布）

- Linux x86_64, aarch64
- macOS x86_64, aarch64
- Windows x86_64

---

## テスト戦略

### 🧪 テストレベル

#### 1. 単体テスト（80%カバレッジ目標）

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_load_config() {
        let config = Config::load("tests/fixtures/config.toml").unwrap();
        assert_eq!(config.general.language, "ja");
    }
}
```

#### 2. セキュリティテスト

```rust
#[test]
fn test_path_traversal_prevention() {
    let attacks = vec!["../../../etc/passwd", "~/.ssh/id_rsa"];
    for attack in attacks {
        let secure_path = SecurePath::new("/home/user/.claude".into());
        assert!(secure_path.validate(Path::new(attack)).is_err());
    }
}
```

#### 3. 統合テスト

```rust
#[test]
fn test_config_init() {
    let mut cmd = Command::cargo_bin("cldev").unwrap();
    cmd.arg("config").arg("init").arg("--non-interactive")
       .assert().success();
}
```

---

## マイグレーション計画

### 🔄 既存スクリプトからの移行

#### ステップ1: 並行運用期間（2週間）
- 既存スクリプト + cldev両方利用可能
- 新規作業はcldev使用推奨

#### ステップ2: エイリアス移行（1週間）

```bash
# ~/.zshrc
unalias uc 2>/dev/null
unalias lr 2>/dev/null

alias c='cldev'
alias cc='cldev config'
alias cd='cldev dev'
alias cg='cldev git'
```

#### ステップ3: 完全移行（即座）

```bash
# 旧スクリプトアーカイブ
mkdir -p ~/.claude/archived/bash-scripts
mv ~/.claude/scripts/claude ~/.claude/archived/bash-scripts/
```

---

## リスク評価

### 🛡️ セキュリティリスク

| リスク | 深刻度 | 軽減策 |
|--------|--------|--------|
| 設定ファイル漏洩 | 🟡 中 | パーミッション600設定 |
| コマンドインジェクション | 🔴 高 | 入力検証徹底、std::process::Command使用 |
| パストラバーサル | 🟡 中 | パス正規化、~/.claude/配下のみアクセス |

### ⚙️ 技術的リスク

| リスク | 深刻度 | 軽減策 |
|--------|--------|--------|
| i18n複雑性 | 🟡 中 | Phase 1はJSON-based、Phase 4でfluent-rs検討 |
| 自動検出失敗 | 🟡 中 | フォールバック手段、明確エラーメッセージ |
| プラットフォーム互換性 | 🟡 中 | CI/CDで3OSテスト |

---

## 成功基準

### 🎯 Phase別成功基準

#### Phase 1: 基盤構築
- [ ] `cldev config init` で設定ファイル作成
- [ ] 英語・日本語切り替え動作
- [ ] シェル補完動作（Zsh/Bash/Fish）
- [ ] CI/CD動作（GitHub Actions）
- [ ] セキュリティテスト通過

#### Phase 2-5: 各Phase
- [ ] 全29コマンド実装完了
- [ ] 統合テスト通過
- [ ] ドキュメント完全（英語・日本語）

### 📈 最終成功基準

```yaml
機能完全性:
  - [ ] 全29コマンド実装・動作確認
  - [ ] i18n完全対応（英語・日本語）
  - [ ] シェル補完完全対応
  - [ ] 初期設定ウィザード動作

品質基準:
  - [ ] テストカバレッジ80%以上
  - [ ] Clippy警告0件
  - [ ] ドキュメント完全性100%
  - [ ] CI/CD完全動作

配布基準:
  - [ ] Homebrewインストール成功
  - [ ] cargo installインストール成功
  - [ ] 3OS対応確認
```

---

## 📅 スケジュール

### 全体スケジュール（7週間）

```
Week 1: Phase 1-A - コア基盤（CLI + 設定 + セキュリティ）
Week 2: Phase 1-B - 高度機能（i18n + 補完 + 対話的UI）
Week 3-4: Phase 2 - 高頻度コマンド（10コマンド）
Week 5: Phase 3 - 開発フロー（8コマンド）
Week 6: Phase 4 - 技術スタック・分析（8コマンド）
Week 7: Phase 5 - 配布・ドキュメント整備
```

---

## 🎉 まとめ

### プロジェクト概要再掲

```yaml
目的: 3つの独立シェルスクリプト → 1つのRust CLI統合
統合対象: 29コマンド（41 → 29、29%削減）
実装期間: 7週間（Phase 1を2週間に分割）
主要技術: Rust, clap 4.x, JSON-based i18n（Phase 1）, TOML
配布方法: Homebrew, crates.io, GitHub Releases

新機能:
  - cldev config init: 対話的初期設定
  - cldev config edit: 設定ファイルエディタで開く
  - i18n: 英語・日本語完全対応
  - シェル補完: Zsh/Bash/Fish/PowerShell
  - TOML設定: ~/.config/cldev/config.toml
  - CI/CD: GitHub Actions完全自動化

期待効果:
  - コマンド数: 29%削減
  - コマンド長: 77%削減
  - インストール: 80%簡略化
  - 起動速度: 90%高速化
  - 多言語対応: UX大幅向上
```

---

**作成者**: Claude Code
**レビュー待ち**: ユーザー承認
**次回更新予定**: Phase 1完了後
