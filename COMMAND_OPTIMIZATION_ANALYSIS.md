# cldev コマンド最適化分析

**作成日**: 2025-11-07
**目的**: 41コマンドの機能重複・統合可能性を分析し、最適な構成を提案

---

## 📊 現在のコマンド構成（41コマンド）

### カテゴリ1: config（設定管理）- 6コマンド

| コマンド | 機能 | 使用頻度予想 | 優先度 |
|----------|------|--------------|--------|
| `validate` | リンク切れ・重複セクション検証 | 週次 | P0 |
| `maintain` | 月次メンテナンス実行 | 月次 | P1 |
| `analyze` | 使用統計・未使用コマンド特定 | 月次 | P2 |
| `smart` | Context-Aware自動最適化 | 未定 | P2 |
| `check` | 設定健全性チェック | 週次 | P1 |
| `list` | 全コマンド一覧表示 | 日次 | P0 |

**分析**:
- ✅ **統合可能**: `validate` と `check` は機能が重複
  - 提案: `cldev config check` に統合（`--validate`, `--health` フラグで分離）
- ⚠️ **低優先度**: `smart` はまだ未実装・仕様不明確
  - 提案: Phase 1から除外、将来的な拡張機能として保留
- ✅ **維持**: `maintain`, `analyze`, `list` は独立機能として維持

**最適化後**: **4コマンド** (`check`, `maintain`, `analyze`, `list`)

---

### カテゴリ2: dev（開発フロー）- 7コマンド

| コマンド | 機能 | 目標時間 | 使用頻度予想 | 優先度 |
|----------|------|----------|--------------|--------|
| `feature` | 新機能実装フロー | 300s | 日次 | P0 |
| `debug` | 体系的デバッグ | 120s | 日次 | P0 |
| `fix` | 重要バグ修正 | 180s | 週次 | P0 |
| `urgent` | 本番障害対応 | 300s | 稀 | P0 |
| `optimize` | パフォーマンス最適化 | 180s | 週次 | P1 |
| `refactor` | リファクタリング | 240s | 週次 | P1 |
| `research` | 技術調査・学習記録 | 600s | 週次 | P2 |

**分析**:
- ✅ **統合可能**: `fix`, `urgent`, `debug` は全て問題解決系
  - しかし、緊急度・対応フローが異なるため、**独立維持が適切**
- ✅ **維持**: 全7コマンドは独立したユースケースを持つ
- 💡 **改善案**: `research` → `cldev lr new` として学習記録カテゴリに移動？
  - 理由: 技術調査の最終成果物は学習記録なため

**最適化後**: **7コマンド維持**（または `research` を `lr new` に移動して **6コマンド**）

---

### カテゴリ3: git（Git操作）- 5コマンド

| コマンド | 機能 | 目標時間 | 使用頻度予想 | 優先度 |
|----------|------|----------|--------------|--------|
| `commit` | 規約準拠コミット | 30s | 日次 | P0 |
| `branch` | ブランチ作成 | 15s | 日次 | P0 |
| `pr` | GitHub PR作成 | 120s | 週次 | P1 |
| `mr` | GitLab MR作成 | 120s | 週次 | P1 |
| `status` | Git状態確認 + 推奨アクション | 10s | 日次 | P0 |
| `todo` | 統合todo管理 | 45s | 日次 | P1 |
| `update-docs` | ドキュメント管理 | 180s | 週次 | P2 |

**元の計画**: 5コマンド（`todo`, `update-docs` は別カテゴリ想定）

**分析**:
- ✅ **統合可能**: `pr` と `mr` は同一機能（GitHub/GitLab差のみ）
  - 提案: `cldev git merge-request` (エイリアス: `mr`, `pr`) で統合
  - リモート検出で自動判定（`.git/config` から github.com/gitlab.com 判別）
- ✅ **維持**: `commit`, `branch`, `status` は高頻度・独立機能
- ❓ **再検討**: `todo`, `update-docs` は本当にgitカテゴリ？
  - `todo` → 独立カテゴリ or `dev` カテゴリ？
  - `update-docs` → `config` カテゴリ？

**最適化後**: **4コマンド** (`commit`, `branch`, `merge-request`, `status`)

---

### カテゴリ4: quality（品質管理）- 3コマンド

| コマンド | 機能 | 目標時間 | 使用頻度予想 | 優先度 |
|----------|------|----------|--------------|--------|
| `lint` | コード品質チェック | 30s | 日次 | P0 |
| `format` | コード整形 | 15s | 日次 | P0 |
| `test` | テスト実行 | 60s | 日次 | P0 |

**分析**:
- ✅ **完璧**: 3コマンドすべて独立・高頻度・必須
- 💡 **拡張案**: `cldev quality all` で一括実行（lint + format + test）
  - しかし、これは既に `cldev quality lint --fix && cldev quality test` で可能
  - 新コマンド追加の価値は低い

**最適化後**: **3コマンド維持**

---

### カテゴリ5: tech（技術スタック）- 5コマンド

| コマンド | 機能 | 目標時間 | 使用頻度予想 | 優先度 |
|----------|------|----------|--------------|--------|
| `web-dev` | Web開発環境起動 | 30s | 日次 | P1 |
| `web-build` | Webプロダクションビルド | 45s | 週次 | P1 |
| `api-dev` | API開発環境起動 | 25s | 日次 | P1 |
| `mobile-dev` | モバイル開発環境起動 | 60s | 週次 | P2 |
| `ds-notebook` | Jupyter Notebook起動 | 40s | 週次 | P2 |

**分析**:
- ✅ **統合可能**: `web-dev`, `api-dev`, `mobile-dev`, `ds-notebook` は全て「開発環境起動」
  - 提案: `cldev tech start [web|api|mobile|ds]` で統合
  - プロジェクト自動検出があれば `cldev tech start` のみで起動可能
- ⚠️ **重複**: `web-build` は `ops build` と重複
  - 提案: `cldev tech start --build` または `cldev ops build` に統合

**最適化後**: **2コマンド** (`start`, `build` → ただし `build` は `ops` に統合で **1コマンド**)

---

### カテゴリ6: ops（運用）- 3コマンド

| コマンド | 機能 | 目標時間 | 使用頻度予想 | 優先度 |
|----------|------|----------|--------------|--------|
| `build` | ビルド実行 | 120s | 日次 | P0 |
| `deploy` | デプロイ実行 | 180s | 週次 | P1 |
| `serena` | セマンティック解析（MCP統合） | 90s | 週次 | P2 |

**分析**:
- ✅ **維持**: `build`, `deploy` は独立・明確
- ❓ **再検討**: `serena` はMCP統合機能で特殊
  - これは本当に「運用」カテゴリ？
  - `analysis` カテゴリの方が適切では？
  - または `cldev mcp serena` として独立カテゴリ？

**最適化後**: **2コマンド** (`build`, `deploy`) + `serena` は移動検討

---

### カテゴリ7: analysis（分析）- 3コマンド

| コマンド | 機能 | 目標時間 | 使用頻度予想 | 優先度 |
|----------|------|----------|--------------|--------|
| `analyze-code` | コード分析 | 90s | 週次 | P1 |
| `explain` | 技術説明 | 60s | 週次 | P2 |
| `review-mr` | MR/PRレビュー | 300s | 週次 | P1 |

**分析**:
- ✅ **維持**: 3コマンドすべて独立機能
- 💡 **名前統一**: `analyze-code` → `analyze` (シンプル化)
- 💡 **追加検討**: `serena` を `analysis` カテゴリに移動

**最適化後**: **3コマンド維持** (`analyze`, `explain`, `review-mr`) + `serena` 追加で **4コマンド**

---

### カテゴリ8: lr（学習記録）- 8コマンド

| コマンド | 機能 | 使用頻度予想 | 優先度 |
|----------|------|--------------|--------|
| `search` | キーワード検索 | 日次 | P0 |
| `stats` | 統計情報 | 週次 | P0 |
| `recent` | 最近の記録 | 日次 | P0 |
| `tags` | タグ分析 | 週次 | P1 |
| `recommend` | 関連記録推薦 | 週次 | P1 |
| `patterns` | パターン参照 | 月次 | P2 |
| `measure` | 効果測定 | 月次 | P2 |
| `problems` | 問題検出 | 月次 | P2 |

**分析**:
- ✅ **統合可能**: `stats`, `tags`, `patterns`, `measure` は全て「分析・統計」
  - 提案: `cldev lr stats [--tags] [--patterns] [--effectiveness]` で統合
- ✅ **統合可能**: `search`, `recent`, `recommend` は全て「検索・取得」
  - 提案: `cldev lr find [keyword] [--recent N] [--recommend]` で統合
- ✅ **維持**: `problems` は独立機能（異常検出）
- 💡 **追加**: `cldev lr new [topic]` - 新しい学習記録作成（`dev research` から移動）

**最適化後**: **4コマンド** (`find`, `stats`, `problems`, `new`)

---

### カテゴリ9: todo（統合todo管理）- 移動候補

| コマンド | 機能 | 目標時間 | カテゴリ候補 |
|----------|------|----------|--------------|
| `todo` | 統合todo管理 | 45s | 独立 or `dev` |

**分析**:
- ❓ **カテゴリ再検討**: `git` カテゴリではなく独立が適切
  - 提案: `cldev todo [add|list|complete]` として独立カテゴリ
  - または `cldev dev todo` として開発フローに統合

**最適化後**: **独立カテゴリ `todo`** として1コマンド

---

### カテゴリ10: docs（ドキュメント管理）- 移動候補

| コマンド | 機能 | 目標時間 | カテゴリ候補 |
|----------|------|----------|--------------|
| `update-docs` | ドキュメント管理 | 180s | 独立 or `config` |

**分析**:
- ❓ **カテゴリ再検討**: `git` カテゴリではなく `config` が適切
  - 提案: `cldev config update-docs` として設定管理に統合

**最適化後**: `config` カテゴリに統合（`config` は5コマンドに）

---

## 📋 最適化案サマリー

### 🔴 現在の構成（41コマンド、8カテゴリ）

```yaml
config: 6コマンド
dev: 7コマンド
git: 5コマンド (+ todo, update-docs)
quality: 3コマンド
tech: 5コマンド
ops: 3コマンド
analysis: 3コマンド
lr: 8コマンド
---
合計: 41コマンド
```

### 🟢 最適化後の構成（28コマンド、8カテゴリ）

```yaml
config: 5コマンド
  - check (validate + check統合、--validate/--health フラグ)
  - maintain
  - analyze
  - list
  - update-docs (gitから移動)

dev: 7コマンド (変更なし)
  - feature
  - debug
  - fix
  - urgent
  - optimize
  - refactor
  - research (または lr new に移動)

git: 4コマンド
  - commit
  - branch
  - merge-request (pr + mr統合、自動検出)
  - status

quality: 3コマンド (変更なし)
  - lint
  - format
  - test

tech: 1コマンド
  - start [web|api|mobile|ds] (自動検出対応)

ops: 2コマンド
  - build
  - deploy

analysis: 4コマンド
  - analyze (analyze-code → 短縮)
  - explain
  - review-mr
  - serena (opsから移動)

lr: 4コマンド
  - find [keyword] (search, recent, recommend統合)
  - stats (stats, tags, patterns, measure統合)
  - problems
  - new [topic] (dev researchから移動？)

todo: 1コマンド (独立カテゴリ)
  - manage [add|list|complete]
---
合計: 28コマンド（32%削減）
```

---

## 🎯 削減・統合の詳細

### ✂️ 削減されたコマンド（13コマンド）

1. **config validate** → `config check --validate` に統合
2. **config smart** → Phase 1から除外（将来拡張）
3. **git pr** → `git merge-request` に統合（自動検出）
4. **git mr** → `git merge-request` に統合（自動検出）
5. **tech web-dev** → `tech start web` に統合
6. **tech web-build** → `ops build` に統合
7. **tech api-dev** → `tech start api` に統合
8. **tech mobile-dev** → `tech start mobile` に統合
9. **tech ds-notebook** → `tech start ds` に統合
10. **lr search** → `lr find` に統合
11. **lr recent** → `lr find --recent` に統合
12. **lr recommend** → `lr find --recommend` に統合
13. **lr tags** → `lr stats --tags` に統合
14. **lr patterns** → `lr stats --patterns` に統合
15. **lr measure** → `lr stats --effectiveness` に統合

**実質削減**: 15コマンド → 統合により **13コマンド削減**

### 🔄 移動されたコマンド（3コマンド）

1. **git todo** → 独立カテゴリ `todo manage`
2. **git update-docs** → `config update-docs`
3. **ops serena** → `analysis serena`
4. **dev research** → `lr new` （検討中）

---

## 💡 最適化のメリット

### ユーザー体験の向上

```bash
# Before（現在の計画）
cldev git pr              # GitHub専用
cldev git mr              # GitLab専用
cldev lr search "暗号化"
cldev lr recent 5
cldev tech web-dev
cldev tech web-build

# After（最適化後）
cldev git merge-request   # 自動検出（エイリアス: pr, mr）
cldev lr find "暗号化" --recent 5
cldev tech start          # 自動検出（web/api/mobile/ds）
cldev ops build           # web-build統合
```

### 実装・保守コストの削減

```yaml
削減効果:
  - コマンド数: 41 → 28 (32%削減)
  - カテゴリ数: 8 → 9 (todo独立で+1)
  - テストケース: 41 → 28 (32%削減)
  - ドキュメント: 41 → 28 (32%削減)
  - 保守コスト: 大幅削減

複雑性削減:
  - tech カテゴリ: 5コマンド → 1コマンド (80%削減)
  - lr カテゴリ: 8コマンド → 4コマンド (50%削減)
  - git カテゴリ: 5コマンド → 4コマンド (20%削減)
```

### Phase 1実装の簡略化

```yaml
Phase 1必須コマンド（Before）:
  - config validate
  - config list
  合計: 2コマンド

Phase 1必須コマンド（After）:
  - config check (--validate, --health)
  - config list
  合計: 2コマンド（実質同じ）

Phase 2必須コマンド（Before）:
  - git commit, branch, pr, mr, status
  - quality lint, format, test
  - dev urgent, fix, debug
  合計: 11コマンド

Phase 2必須コマンド（After）:
  - git commit, branch, merge-request, status
  - quality lint, format, test
  - dev urgent, fix, debug
  合計: 10コマンド（9%削減）
```

---

## ⚠️ 注意事項・トレードオフ

### 統合による複雑性

```rust
// Before: 単純な2コマンド
cldev git pr
cldev git mr

// After: 条件分岐が必要
cldev git merge-request
  ↓
リモートURL検出 → github.com or gitlab.com
  ↓
gh pr create or glab mr create
```

**対策**:
- プロジェクト検出ロジックの堅牢性確保
- エラーメッセージの明確化
- フォールバック手段の提供

### オプション爆発の危険

```bash
# Before: シンプル
cldev lr search "暗号化"
cldev lr recent 5

# After: オプション多数
cldev lr find "暗号化" --recent 5 --recommend --tags security
```

**対策**:
- デフォルト動作の明確化
- ヘルプメッセージの充実
- 短縮オプション提供（`-r`, `-t`等）

---

## 🎯 推奨アクション

### 即座実施推奨（影響小・効果大）

1. ✅ **git pr/mr統合** → `merge-request`（自動検出）
2. ✅ **tech統合** → `tech start [type]`（自動検出）
3. ✅ **config validate/check統合** → `check --validate`
4. ✅ **analysis名前短縮** → `analyze-code` → `analyze`

### 慎重検討推奨（影響大）

1. ⚠️ **lr統合** → `find`, `stats` 2コマンドへ
   - 理由: 8→4は大幅削減だが、オプション複雑化
   - 提案: ユーザーフィードバック収集後に判断

2. ⚠️ **dev research → lr new移動**
   - 理由: カテゴリ境界が曖昧
   - 提案: 並行運用期間で使用実態確認

### Phase 1から除外推奨

1. ❌ **config smart** → 仕様不明確、将来拡張
2. ❌ **lr patterns, measure** → 使用頻度低、Phase 4以降

---

## 📊 最終推奨構成

```yaml
【最小構成】28コマンド、9カテゴリ

config (5):
  - check, maintain, analyze, list, update-docs

dev (7):
  - feature, debug, fix, urgent, optimize, refactor, research

git (4):
  - commit, branch, merge-request, status

quality (3):
  - lint, format, test

tech (1):
  - start

ops (2):
  - build, deploy

analysis (4):
  - analyze, explain, review-mr, serena

lr (4):
  - find, stats, problems, new

todo (1):
  - manage
```

**削減率**: 41 → 28コマンド（**32%削減**）
**保守性**: 大幅向上（テスト・ドキュメント削減）
**UX**: 向上（自動検出、統合コマンド）

---

## 🔄 次のステップ

1. **ユーザー承認**: この最適化案をレビュー・承認
2. **実装計画書更新**: 28コマンド構成で再作成
3. **Phase 1実装**: 最適化後の構成で開始

---

**作成者**: Claude Code
**レビュー待ち**: ユーザー承認
